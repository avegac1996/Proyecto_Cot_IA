name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  backend-tests:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_DB: cotia_test
          POSTGRES_USER: cotia_user
          POSTGRES_PASSWORD: cotia_pass
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Install dependencies
        working-directory: ./BACKEND
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest pytest-asyncio

      - name: Run tests
        working-directory: ./BACKEND
        env:
          DATABASE_URL: postgresql+asyncpg://cotia_user:cotia_pass@localhost:5432/cotia_test
        run: pytest -v

  frontend-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Install dependencies
        working-directory: ./FRONTEND
        run: npm ci

      - name: Lint
        working-directory: ./FRONTEND
        run: npx tsc --noEmit

      - name: Build
        working-directory: ./FRONTEND
        run: npm run build
